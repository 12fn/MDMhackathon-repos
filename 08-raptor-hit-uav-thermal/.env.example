# Today: hits real OpenAI directly
OPENAI_API_KEY=sk-...
OPENAI_PRIMARY_MODEL=gpt-5.4-mini
OPENAI_FALLBACK_MODELS=gpt-5-mini,gpt-4o-mini,gpt-4o

# Hero vision call uses a vision-capable model (any one works as long as it accepts image_url)
RAPTOR_HERO_MODEL=gpt-4o

# Tomorrow: swap to Kamiwaza on-prem with TWO env vars. Same code path.
# KAMIWAZA_BASE_URL=https://kamiwaza.local/api/v1
# KAMIWAZA_API_KEY=...
