# Today: hits OpenAI directly.
OPENAI_API_KEY=sk-...
OPENAI_PRIMARY_MODEL=gpt-5.4-mini
OPENAI_FALLBACK_MODELS=gpt-5-mini,gpt-4o-mini,gpt-4o
OPENAI_HERO_MODEL=gpt-5.4

# Tomorrow: same code, set these and the entire orchestration stays inside the wire.
# KAMIWAZA_BASE_URL=https://kamiwaza.local/api/v1
# KAMIWAZA_API_KEY=...

# Optional knobs:
# OMNI_LIVE_WARM=1            # have data/generate.py call the real LLM during pre-warm
