# Today: hits OpenAI directly.
OPENAI_API_KEY=sk-...
OPENAI_PRIMARY_MODEL=gpt-5.4-mini
OPENAI_FALLBACK_MODELS=gpt-5-mini,gpt-4o-mini,gpt-4o

# Tomorrow: same code, set these and we are inside the wire.
# KAMIWAZA_BASE_URL=https://kamiwaza.local/api/v1
# KAMIWAZA_API_KEY=...

# Real-data plug-in (optional). Drop a GCSS-MC work-order CSV at this path
# and `data/load_real.py` will route it through the same pipeline.
# REAL_DATA_PATH=/path/to/gccsmc_workorders.csv
